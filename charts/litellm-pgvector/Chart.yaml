apiVersion: v2
name: litellm-pgvector
description: Helm chart for deploying the LiteLLM + pgvector sample service.
type: application
version: 0.1.0
appVersion: "0.1.0"
dependencies:
  - name: postgresql
    version: 15.5.17
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
