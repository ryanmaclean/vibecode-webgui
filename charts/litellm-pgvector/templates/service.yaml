apiVersion: v1
kind: Service
metadata:
  name: {{ include "litellm-pgvector.fullname" . }}
  labels:
    {{- include "litellm-pgvector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "litellm-pgvector.selectorLabels" . | nindent 4 }}
