version: '3.8'

services:
<<<<<<< HEAD
  # Documentation Service (Astro + Starlight)
  docs:
    build:
      context: ./docs
      dockerfile: Dockerfile
    container_name: vibecode-docs
    ports:
      - "8080:8080"
    environment:
      - NODE_ENV=production
      - DD_SERVICE=vibecode-docs
      - DD_ENV=${ENVIRONMENT:-dev}
      - DD_VERSION=1.0.0
      - DD_TAGS=env:${ENVIRONMENT:-dev},service:vibecode-docs,component:docs
    depends_on:
      - datadog-agent
    networks:
      - vibecode-network
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:8080/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.docs.rule=Host(`docs.localhost`)"
      - "traefik.http.services.docs.loadbalancer.server.port=8080"
      # Datadog log processing
      - "com.datadoghq.ad.logs=[{\"source\": \"nginx\", \"service\": \"vibecode-docs\"}]"

  # Main VibeCode Application
  app:
    build:
      context: .
      dockerfile: Dockerfile
      target: runner
    container_name: vibecode-app
=======
  # VibeCode WebGUI Application
  webgui:
    build: .
>>>>>>> merge-conflict-cleanup
    ports:
      - "3000:3000"
    env_file:
      - .env.docker
    environment:
      - DATABASE_URL=postgresql://postgres:password@postgres:5432/vibecode
      - NEXTAUTH_SECRET=your-nextauth-secret-here
      - NEXTAUTH_URL=http://localhost:3000
<<<<<<< HEAD
      
      # Datadog RUM (Real User Monitoring) variables
      # Prefer NEXT_PUBLIC_DD_*; map from legacy NEXT_PUBLIC_DATADOG_* if DD_* is unset
      - NEXT_PUBLIC_DD_APPLICATION_ID=${NEXT_PUBLIC_DD_APPLICATION_ID:-${NEXT_PUBLIC_DATADOG_APPLICATION_ID}}
      - NEXT_PUBLIC_DD_CLIENT_TOKEN=${NEXT_PUBLIC_DD_CLIENT_TOKEN:-${NEXT_PUBLIC_DATADOG_CLIENT_TOKEN}}
      - NEXT_PUBLIC_DD_SITE=${NEXT_PUBLIC_DD_SITE:-${NEXT_PUBLIC_DATADOG_SITE:-datadoghq.com}}
      
      # Datadog APM (Application Performance Monitoring)
      - DD_AGENT_HOST=datadog-agent
      - DD_TRACE_AGENT_PORT=8126
      - DD_SERVICE=vibecode-app
      - DD_ENV=${ENVIRONMENT:-dev}
      - DD_VERSION=1.0.0
      - DD_PROFILING_ENABLED=true
      - DD_LOGS_INJECTION=true
      - DD_RUNTIME_METRICS_ENABLED=true
      
      # Database monitoring integration
      - DD_DATABASE_MONITORING_ENABLED=true
      - DD_DBM_PROPAGATION_MODE=full
      
      # Tags for better organization
      - DD_TAGS=env:${ENVIRONMENT:-dev},service:vibecode-app,component:web
=======
      - NODE_ENV=production
>>>>>>> merge-conflict-cleanup
    depends_on:
      - postgres
      - redis
    restart: unless-stopped

<<<<<<< HEAD
  # PostgreSQL Database with pgvector support
  db:
    image: pgvector/pgvector:pg15
    container_name: vibecode-db
    ports:
      - "5433:5432"
=======
  # PostgreSQL Database
  postgres:
    image: postgres:16
>>>>>>> merge-conflict-cleanup
    environment:
      - POSTGRES_DB=vibecode
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=password
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./prisma/schema.prisma:/docker-entrypoint-initdb.d/schema.prisma
    ports:
      - "5432:5432"
    restart: unless-stopped

  # Redis for caching and sessions
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
<<<<<<< HEAD
    networks:
      - vibecode-network
    healthcheck:
      test: ["CMD", "valkey-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Authelia Authentication Server
  authelia:
    image: authelia/authelia:4.38.10
    container_name: vibecode-authelia
    ports:
      - "9091:9091"
    environment:
      - AUTHELIA_LOG_LEVEL=info
      - AUTHELIA_JWT_SECRET=${AUTHELIA_JWT_SECRET:-insecure_jwt_secret_change_in_production}
      - AUTHELIA_SESSION_SECRET=${AUTHELIA_SESSION_SECRET:-insecure_session_secret_change_in_production}
      - AUTHELIA_STORAGE_ENCRYPTION_KEY=${AUTHELIA_STORAGE_ENCRYPTION_KEY:-vibecode-storage-encryption-key-change-in-production}
      - AUTHELIA_STORAGE_POSTGRES_HOST=db
      - AUTHELIA_STORAGE_POSTGRES_DATABASE=vibecode
      - AUTHELIA_STORAGE_POSTGRES_USERNAME=vibecode
      - AUTHELIA_STORAGE_POSTGRES_PASSWORD=password
      - AUTHELIA_SESSION_REDIS_HOST=redis
      - AUTHELIA_SESSION_REDIS_PORT=6379
      - AUTHELIA_NOTIFIER_FILESYSTEM_FILENAME=/config/notification.txt
    volumes:
      - ./docker/authelia/configuration.yml:/config/configuration.yml:ro
      - ./docker/authelia/users_database.yml:/config/users_database.yml:ro
      - authelia_data:/config
    networks:
      - vibecode-network
    depends_on:
      - db
      - redis
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:9091/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.authelia.rule=Host(`auth.localhost`)"
      - "traefik.http.services.authelia.loadbalancer.server.port=9091"
      # Datadog log processing
      - "com.datadoghq.ad.logs=[{\"source\": \"authelia\", \"service\": \"vibecode-authelia\"}]"

  # Code Server for Development Environment
  code-server:
    image: codercom/code-server:latest
    container_name: vibecode-code-server
    ports:
      - "8080:8080"
    environment:
      - PASSWORD=vibecode123
    volumes:
      - ./workspaces:/home/coder/workspaces
      - code_server_data:/home/coder/.local/share/code-server
    networks:
      - vibecode-network

  # Docker Model Runner Integration
  ai-model-runner:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    restart: unless-stopped

  # MCP Server with Model Runner Integration
  mcp-servers:
    build:
      context: .
      dockerfile: ./docker/mcp-servers/Dockerfile
    container_name: vibecode-mcp-servers
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - ./src:/workspace/src:ro
      - ./public:/workspace/public:ro
      - model-runner-cache:/app/models
    environment:
      - DOCKER_MODEL_RUNNER_URL=http://model-runner.docker.internal/engines/v1
      - MCP_SERVER_PORT=3001
      - WORKSPACE_PATH=/workspace
      - AVAILABLE_MODELS=ai/smollm2:360M-Q4_K_M,ai/llama3.2:1b-Q4_K_M,ai/qwen2.5-coder:1.5b-Q4_K_M
    ports:
      - "3001:3001"
      - "3002:3002"
      - "3003:3003"
    networks:
      - vibecode-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3001/health"]
      interval: 30s
      timeout: 10s
      retries: 3

  # Voice Processing Service  
  voice-processor:
    image: node:20-alpine
    container_name: vibecode-voice-processor
    working_dir: /app
    volumes:
      - ./docker/voice-processor:/app
      - voice-uploads:/app/uploads
      - free-llm-models:/app/runtime/free-llm-models
    ports:
      - "3004:3004"
    networks:
      - vibecode-network
    command: >
      sh -c "mkdir -p /app/runtime/free-llm-models && while true; do node -r dd-trace/init scripts/jobs/update-openrouter-free-models.js && sleep 43200; done"
    environment:
      - MODEL_RUNNER_URL=http://model-runner.docker.internal/engines/v1
      - SPEECH_MODEL=ai/whisper:base-Q4_K_M
      - OUTPUT_JSON_PATH=/app/runtime/free-llm-models/models.json
      - OUTPUT_TEXT_PATH=/app/runtime/free-llm-models/models.txt
      - ENABLE_CONFIGMAP_SYNC=false
      - MAX_FREE_MODELS=10
      - MIN_WORKING_MODELS=3
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY:-}
      - OPENROUTER_HTTP_REFERER=${OPENROUTER_HTTP_REFERER:-}
      - OPENROUTER_APP_TITLE=${OPENROUTER_APP_TITLE:-}
      - DD_API_KEY=${DD_API_KEY:-}
      - DD_SITE=${DD_SITE:-datadoghq.com}
      - DD_ENV=${DD_ENV:-dev}
      - DD_DBM_PROPAGATION_MODE=full
      - DD_SERVICE=vibecode-webgui
      - DD_VERSION=0.1.0-dev
      - DATADOG_METRIC_TAGS=environment:dev
    restart: unless-stopped

  # Datadog Agent for Local Development
  # Provides dev/stg/prd parity for monitoring
  datadog-agent:
    image: datadog/docker-dd-agent:latest-alpine
    container_name: vibecode-datadog-agent
    environment:
      - DD_API_KEY=${DD_API_KEY:-${DATADOG_API_KEY:-dummy-key-for-local-dev}}
      - DD_APP_KEY=${DD_APP_KEY}
      - DD_SITE=${DD_SITE:-${DATADOG_SITE:-datadoghq.com}}
      - DD_HOSTNAME=vibecode-docker-compose
      - DD_ENV=${ENVIRONMENT:-dev}
      - DD_SERVICE=vibecode-webgui
      - DD_VERSION=1.0.0
      
      # Enable core monitoring features
      - DD_LOGS_ENABLED=true
      - DD_LOGS_CONFIG_CONTAINER_COLLECT_ALL=true
      - DD_APM_ENABLED=true
      - DD_APM_NON_LOCAL_TRAFFIC=true
      - DD_PROCESS_AGENT_ENABLED=true
      - DD_DOCKER_ENABLED=true
      
      # Database monitoring
      - DD_DATABASE_MONITORING_ENABLED=true
      
      # Local development optimizations
      - DD_PROCESS_CONFIG_PROCESS_COLLECTION_ENABLED=true
      - DD_LOG_LEVEL=${DD_LOG_LEVEL:-INFO}
      
      # Container insights
      - DD_CONTAINER_INCLUDE=name:vibecode.*
      - DD_CONTAINER_EXCLUDE=name:datadog.*
      
      # Tags for local environment
      - DD_TAGS=env:${ENVIRONMENT:-dev},cluster:docker-compose,project:vibecode
    volumes:
      # Essential Docker socket for container monitoring
      - /var/run/docker.sock:/var/run/docker.sock:ro
      - /proc/:/host/proc/:ro
      - /sys/fs/cgroup/:/host/sys/fs/cgroup:ro
      - /etc/passwd:/etc/passwd:ro
      
      # APM traces directory
      - datadog-apm:/opt/datadog-agent/run
    ports:
      - "8126:8126"  # APM traces
      - "8125:8125/udp"  # DogStatsD metrics
    networks:
      - vibecode-network
    labels:
      - "com.datadoghq.ad.check_names=[\"docker\"]"
      - "com.datadoghq.ad.init_configs=[{}]"
      - "com.datadoghq.ad.instances=[{\"docker_root\":\"/host\"}]"
    healthcheck:
      test: ["CMD", "agent", "health"]
      interval: 30s
      timeout: 10s
      retries: 3
=======
>>>>>>> merge-conflict-cleanup
    restart: unless-stopped

  # Nginx reverse proxy
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - webgui
    restart: unless-stopped

volumes:
  postgres_data:
<<<<<<< HEAD
  redis_data:
    driver: local
  authelia_data:
    driver: local
  code_server_data:
    driver: local
  workspaces:
    driver: local
  node_modules:
    driver: local
  git-cache:
    driver: local
  model-cache:
    driver: local
  model-runner-cache:
    driver: local
  voice-uploads:
    driver: local
  datadog-apm:
    driver: local
  free-llm-models:
    driver: local

networks:
  vibecode-network:
    driver: bridge
=======
  redis_data:
>>>>>>> merge-conflict-cleanup
