apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vibecode-pgvector.fullname" . }}-test-connection"
  labels:
    {{- include "vibecode-pgvector.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "vibecode-pgvector.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
