name: Main Branch CI (Lightweight)

<<<<<<< HEAD
# Optimized for cost - only essential checks on main branch
# Full testing happens on release branches

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
=======
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
>>>>>>> merge-conflict-cleanup

concurrency:
  group: main-ci-${{ github.ref }}
  cancel-in-progress: true

<<<<<<< HEAD
env:
  NODE_VERSION: '20.11.0'  # Match package.json engines requirement

jobs:
  # Fast validation - essential checks only
=======
permissions:
  contents: read
  security-events: write

env:
  NODE_VERSION: '20.11.0'

jobs:
>>>>>>> merge-conflict-cleanup
  quick-validation:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
<<<<<<< HEAD
      - name: Checkout code
        uses: actions/checkout@v4

=======
      - uses: actions/checkout@v4
>>>>>>> merge-conflict-cleanup
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
<<<<<<< HEAD
        run: npm ci --prefer-offline --no-audit --legacy-peer-deps

      - name: Lint code
        run: npm run lint || true  # Don't fail on lint warnings

      - name: Type check
        run: npm run type-check || true

      - name: Run fast unit tests only
        run: |
          npm test -- --passWithNoTests \
            --testPathIgnorePatterns="tests/integration|tests/e2e|tests/k8s|tests/performance" \
            --maxWorkers=2 \
            --testTimeout=10000
        env:
          NODE_ENV: test
          CI: true

  # Security scan - lightweight version
  security-check:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2  # Fetch HEAD and parent for comparison

      - name: Run npm audit
        run: npm audit --audit-level=high || true
        continue-on-error: true

      - name: Check for hardcoded secrets
        run: |
          echo "Scanning for hardcoded secrets and API keys..."
          # Enhanced secret scanning with multiple patterns
          if grep -r -E "(sk-[a-zA-Z0-9]{40,}|sk-ant-[a-zA-Z0-9]{40,}|ghp_[a-zA-Z0-9]{36}|AKIA[0-9A-Z]{16})" src/ --exclude-dir=node_modules 2>/dev/null; then
            echo "WARNING: Potential secrets found"
            exit 1
          fi
          echo "✅ No hardcoded secrets found"

  # Build validation - ensure it compiles
  build-check:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --prefer-offline --no-audit --legacy-peer-deps

      - name: Build application
        run: npm run build
        env:
          NODE_ENV: production
          SKIP_ENV_VALIDATION: true

  # Cost monitoring - track usage
  cost-monitoring:
    runs-on: ubuntu-latest
    needs: [quick-validation, security-check, build-check]
    if: always() && github.event_name == 'push' && github.ref == 'refs/heads/main'
    steps:
      - name: Log workflow execution
        run: |
          echo "Main branch CI executed at $(date)"
          echo "Estimated cost: ~$0.05 (3 jobs, ~25 minutes total)"
          echo "For full testing, create a release/* branch"
          echo "Jobs status:"
          echo "  - quick-validation: ${{ needs.quick-validation.result }}"
          echo "  - security-check: ${{ needs.security-check.result }}"
          echo "  - build-check: ${{ needs.build-check.result }}"
=======
        run: npm ci

      - name: Lint check
        run: npm run lint || echo "Linting issues found (non-blocking)"

      - name: Type check
        run: npm run type-check || echo "Type issues found (non-blocking)"

      - name: Basic unit tests
        run: npm test -- --testPathPattern="(auth|security|utils)" --passWithNoTests
        timeout-minutes: 5

  security-scan:
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v4
      - name: Run security audit
        run: npm audit --audit-level moderate || echo "Security issues found (logged)"

      - name: Check for secrets
        uses: trufflesecurity/trufflehog@main
        with:
          path: ./
          base: main
          head: HEAD
        continue-on-error: true

  cost-monitor:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Log cost optimization
        run: |
          echo "💰 Cost optimization active: Main branch runs lightweight CI only"
          echo "📊 Expected savings: 70-80% reduction in GitHub Actions usage"
          echo "🚀 For full testing, create a release branch: git checkout -b release/v1.0.0"
>>>>>>> merge-conflict-cleanup
