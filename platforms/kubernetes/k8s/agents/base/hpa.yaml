apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: openai-agents-hpa
  namespace: openai-agents
  labels:
    app: openai-agents
    component: autoscaling
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: openai-agents
  minReplicas: 3
  maxReplicas: 20
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80
  - type: Pods
    pods:
      metric:
        name: agent_active_sessions
      target:
        type: AverageValue
        averageValue: "8"
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 300
      policies:
      - type: Percent
        value: 50
        periodSeconds: 60
      - type: Pods
        value: 2
        periodSeconds: 60
      selectPolicy: Min
    scaleUp:
      stabilizationWindowSeconds: 60
      policies:
      - type: Percent
        value: 100
        periodSeconds: 30
      - type: Pods
        value: 4
        periodSeconds: 30
      selectPolicy: Max
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: openai-agents-custom-hpa
  namespace: openai-agents
  labels:
    app: openai-agents
    component: autoscaling
    scaling-strategy: custom
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: openai-agents
  minReplicas: 3
  maxReplicas: 20
  metrics:
  - type: External
    external:
      metric:
        name: agent_request_queue_depth
        selector:
          matchLabels:
            app: openai-agents
      target:
        type: AverageValue
        averageValue: "10"
  - type: External
    external:
      metric:
        name: agent_response_time_p95
        selector:
          matchLabels:
            app: openai-agents
      target:
        type: Value
        value: "2000"
