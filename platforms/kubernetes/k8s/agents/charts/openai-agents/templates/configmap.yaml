apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "openai-agents.fullname" . }}-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "openai-agents.labels" . | nindent 4 }}
data:
  agent.yaml: |
    {{- toYaml .Values.agentConfig | nindent 4 }}
