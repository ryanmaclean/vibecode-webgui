version: '3.8'

services:
  vibechat:
    image: vibecodecr6c3db0e6.azurecr.io/vibechat-ddtrace:202509200930
    expose:
      - "3000"
    environment:
      - PORT=3000
      - HOSTNAME=0.0.0.0
    depends_on:
      - datadog
  datadog:
    image: datadog/serverless-init:latest
    expose:
      - "8126"
    restart: always
