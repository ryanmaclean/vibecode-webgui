# Allow optional dependencies for platform-specific binaries
# optional=false

# Use exact versions to avoid license issues
save-exact=true

# Audit level for security
audit-level=moderate

# Resolve Datadog peer dependency mismatch consistently
legacy-peer-deps=true
engine-strict=false
