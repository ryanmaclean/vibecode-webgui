# Disable optional dependencies that might include LGPL packages
optional=false

# Disable Sharp image optimization completely
sharp=false

# Use exact versions to avoid license issues
save-exact=true

# Audit level for security
audit-level=moderate